#include <stdio.h>
#include<string.h>
void main()
{
    char ptr[50]={};
    printf("Enter the expression using +, -, *, / operators :\n");
    scanf("%s",&ptr);
    printf("%s\n",ptr);
    printf("%d",ptr);

}